# MadGamma (MedGemma Inference)

โปรเจกต์นี้สำหรับรันโมเดล `google/medgemma-4b-it` เพื่อวิเคราะห์ภาพ X-ray และสรุปผลลัพธ์ทางการแพทย์

## ความต้องการของระบบ (Prerequisites)

ตรวจสอบว่าได้ติดตั้ง Python และ library ที่จำเป็นแล้ว:

```bash
pip3 install transformers torch pillow
```

**ข้อกำหนดสำคัญ:**
- Python 3.13+
- PyTorch 2.9+ (สำหรับ MPS support ที่ดีที่สุด)
- macOS with Apple Silicon (สำหรับ MPS acceleration)

## การใช้งาน (Usage)

### ⚠️ สำคัญ: ใช้ `python3` แทน Anaconda Python

โปรแกรมนี้ทำงานได้ดีที่สุดกับ `python3` ของระบบ (PyTorch 2.9.1)  
**หลีกเลี่ยง** การใช้ `/opt/anaconda3/bin/python` เพราะ PyTorch 2.8.0 มีบั๊กกับ MPS

### 1. ใช้ Shell Script (แนะนำ)
รันคำสั่งด้านล่างเพื่อเริ่มโปรแกรม:
```bash
./run_madgamma.sh
```

### 2. รันด้วย Python โดยตรง
คุณสามารถระบุ path ของไฟล์รูปภาพ X-ray ที่ต้องการวิเคราะห์ได้ทันที:
```bash
python3 main.py path/to/your/image.jpg
```

ตัวอย่าง:
```bash
python3 main.py "/Users/phoom/Desktop/Machine learning/Model AI/madgamma/LungCACXR.PNG"
```

หรือรันโดยไม่ระบุไฟล์ (โปรแกรมจะถาม path หรือใช้ค่าเริ่มต้น `xray_sample.jpg`):
```bash
python3 main.py
```

## การทำงานของโปรแกรม
1. ตรวจสอบว่า MPS (Metal Performance Shaders) พร้อมใช้งานหรือไม่
2. โหลดโมเดล `google/medgemma-4b-it` ด้วย `bfloat16` precision บน MPS
3. รับไฟล์รูปภาพ (X-ray)
4. ส่ง prompt "Analyze this X-ray and summarize the findings." ไปยังโมเดล
5. แสดงผลลัพธ์การวิเคราะห์ออกมาทางหน้าจอ

## การเพิ่มประสิทธิภาพ
- ใช้ **MPS acceleration** บน Apple Silicon → เร็วกว่า CPU มาก
- ใช้ **bfloat16** precision → ลดการใช้หน่วยความจำจาก 16GB เหลือ 8GB
- รองรับทั้ง **image analysis** และ **text-only** mode

## Troubleshooting

### ❌ Segmentation Fault
**สาเหตุ:** ใช้ Anaconda Python ที่มี PyTorch เวอร์ชันเก่า

**วิธีแก้:**
```bash
# วิธีที่ 1: Deactivate Anaconda ก่อนรัน (แนะนำ)
conda deactivate
python3 main.py image.png

# วิธีที่ 2: อัพเดท PyTorch ใน Anaconda
conda install pytorch torchvision torchaudio -c pytorch
```

**⚠️ สำคัญ:** หลัง `conda deactivate` ให้ปิด Terminal แล้วเปิดใหม่ เพราะบางครั้ง environment variables ยังค้างอยู่

### ❌ ไฟล์มีช่องว่างในชื่อ
**ปัญหา:** ชื่อไฟล์อย่าง `chest-x-ray results.jpg` จะถูกแปลเป็น 2 ไฟล์แยกกัน

**วิธีแก้:**
```bash
# ใช้ quotes ครอบชื่อไฟล์
python3 main.py "chest-x-ray results.jpg"

# หรือใช้ path เต็ม
python3 main.py "/Users/phoom/Desktop/chest-x-ray results.jpg"

# หรือเปลี่ยนชื่อไฟล์ให้ไม่มีช่องว่าง (แนะนำ)
mv "chest-x-ray results.jpg" chest-xray-results.jpg
python3 main.py chest-xray-results.jpg
```

### 📊 เปรียบเทียบ Python Environments

| Python | PyTorch Version | สถานะ |
|--------|----------------|-------|
| `/opt/anaconda3/bin/python` | 2.8.0 | ❌ Segmentation Fault |
| `python3` (หลัง conda deactivate) | 2.9.1 | ✅ ใช้งานได้ปกติ |

### 🔍 ตรวจสอบ Python Environment

```bash
# ดูว่าใช้ Python ตัวไหน
which python3

# ควรได้ผลลัพธ์:
# /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 ✅
# หรือ /usr/local/bin/python3 ✅
# หรือ /opt/homebrew/bin/python3 ✅

# ถ้าได้ /opt/anaconda3/bin/python3 ❌
# → ให้รัน: conda deactivate แล้วปิด Terminal เปิดใหม่
```

## หมายเหตุ
- หากไม่พบไฟล์รูปภาพ โปรแกรมจะทำงานในโหมด Text-only (ถามคำถามทั่วไปเกี่ยวกับไข้หวัดใหญ่แทน)
- โปรแกรมใช้ `bfloat16` เพื่อความเสถียรทางตัวเลขและประหยัดหน่วยความจำ
- รองรับ MPS (Metal Performance Shaders) สำหรับ Apple Silicon Macs
