# 🚀 วิธีรัน Drugstore Canary

## ระบบที่ 1: ระบบตรวจจับจากยอดขายยา (Sales-Based)

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
cd "Drugstore Canary"
pip3 install -r requirements.txt
```

### ขั้นตอนที่ 2: สร้างข้อมูลทดสอบ

```bash
python3 data/data_generator.py
```

ผลลัพธ์:
```
✅ Successfully generated synthetic data!
   - Zones: 4
   - Pharmacies: 10
   - Days of data: 365
   - Outbreak scenarios: 2
```

### ขั้นตอนที่ 3: เปิด API Server

```bash
python3 api/main.py
```

เปิดที่: http://localhost:8000
API Docs: http://localhost:8000/docs

### ขั้นตอนที่ 4: เปิด Dashboard

เปิด Terminal ใหม่:
```bash
cd "Drugstore Canary/dashboard"
python3 -m http.server 8080
```

เปิดเว็บ: http://localhost:8080

---

## ระบบที่ 2: ระบบตรวจจับจากสต็อกหมด (Stock Monitoring Bot)

### ขั้นตอนที่ 1: ติดตั้ง Playwright

```bash
pip3 install playwright
playwright install chromium
```

### ขั้นตอนที่ 2: ทดสอบ Bot

```bash
python3 demo_bot.py
```

ผลลัพธ์:
```
✅ Browser initialized successfully!
✅ Navigation successful!
✅ Screenshot saved
Ready for pharmacy scraping! 🎉
```

### ขั้นตอนที่ 3: รัน Bot แบบครั้งเดียว (ทดสอบ)

```bash
python3 bots/bot_scheduler.py --once --debug
```

### ขั้นตอนที่ 4: รัน Bot แบบต่อเนื่อง (ทุก 2 ชั่วโมง)

```bash
python3 bots/bot_scheduler.py --interval 2
```

### ขั้นตอนที่ 5: ดูผลการวิเคราะห์

```bash
python3 bots/stock_analyzer.py
```

---

## 🎯 Quick Start (เริ่มใช้งานเร็ว)

### แบบที่ 1: ใช้ Script อัตโนมัติ

```bash
chmod +x quick_start.sh
./quick_start.sh
```

### แบบที่ 2: รัน Bot เฉพาะ Grab

```bash
python3 bots/grab_scraper.py
```

### แบบที่ 3: รัน Bot เฉพาะ Lineman

```bash
python3 bots/lineman_scraper.py
```

---

## 📁 ผลลัพธ์จะอยู่ที่ไหน

### ระบบยอดขาย:
- Database: `data/drugstore_canary.db`
- API: http://localhost:8000
- Dashboard: http://localhost:8080

### ระบบ Bot:
- Screenshots: `screenshots/*.png`
- ผลการ Scrape: `bot_results/stock_check_*.json`
- ผลล่าสุด: `bot_results/latest.json`
- รายงานวิเคราะห์: `stock_analysis_report.json`

---

## ⚙️ ปรับแต่ง

### เปลี่ยนยาที่ติดตาม

แก้ไขไฟล์: `bots/key_items.json`

### เปลี่ยนความถี่ในการ Scrape

```bash
# ทุก 4 ชั่วโมง
python3 bots/bot_scheduler.py --interval 4

# ทุก 1 ชั่วโมง
python3 bots/bot_scheduler.py --interval 1
```

### เปลี่ยนโซนพื้นที่

แก้ไขไฟล์: `config.py` → `HAT_YAI_ZONES`

---

## 🔍 ตรวจสอบว่าทำงานหรือไม่

### ตรวจสอบ Database

```bash
python3 -c "from data.database import get_session; print('✅ Database OK')"
```

### ตรวจสอบ ML Models

```bash
python3 models/prophet_detector.py
python3 models/lstm_detector.py
```

### ตรวจสอบ Bot

```bash
python3 demo_bot.py
```

---

## ❌ แก้ปัญหา

### ถ้า Import Error

```bash
pip3 install -r requirements.txt
```

### ถ้า Playwright Error

```bash
pip3 install playwright
playwright install chromium
```

### ถ้า Database Error

```bash
rm data/drugstore_canary.db
python3 data/data_generator.py
```

### ถ้า Bot ถูกบล็อก

- ลด interval เป็น 4-6 ชั่วโมง
- เปิด headless=False เพื่อดูว่าเกิดอะไรขึ้น
- ตรวจสอบ screenshots/ folder

---

## 📊 ตัวอย่างผลลัพธ์

### API Response

```bash
curl http://localhost:8000/api/zones
```

```json
[
  {
    "id": "zone_a",
    "name": "ตัวเมืองหาดใหญ่",
    "center_lat": 7.0087,
    "center_lon": 100.4754,
    "num_pharmacies": 3
  }
]
```

### Bot Results

```json
{
  "platform": "grab",
  "pharmacy_name": "Boots Hat Yai",
  "items": [
    {
      "product_name": "ผงเกลือแร่ ORS",
      "is_available": false,
      "price": "25.00"
    }
  ]
}
```

---

## 🎓 เอกสารเพิ่มเติม

- `README.md` - คู่มือหลัก (English)
- `STOCK_MONITOR_README.md` - คู่มือ Bot
- `SYSTEM_SUMMARY.md` - สรุประบบทั้งหมด
- `walkthrough.md` - รายละเอียดระบบยอดขาย
- `stock_bot_walkthrough.md` - รายละเอียดระบบ Bot

---

## 💡 Tips

1. **ทดสอบก่อน**: ใช้ `--once --debug` ก่อนรันต่อเนื่อง
2. **ดู Screenshots**: ตรวจสอบ `screenshots/` เพื่อ debug
3. **เช็ค Logs**: ดูว่า Bot ทำงานถูกต้องหรือไม่
4. **ปรับ Interval**: ถ้าถูกบล็อก ให้เพิ่มระยะเวลา

---

**สร้างด้วย ❤️ เพื่อการป้องกันโรคระบาดในหาดใหญ่**
